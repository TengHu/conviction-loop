# Your actual book is private — never commit it.
# Copy template/ to book/ and work there; book/ stays local.
book/

# Local Claude Code state / secrets
.claude/settings.json
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Cloned reference repos, scratch
oss/

.DS_Store
*.lock
