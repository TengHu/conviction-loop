# Your actual book is private — never commit it.
# Copy template_book/ to book/ and work there; book/ stays local.
book/

# Local Claude Code state / secrets
.claude/settings.json
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Secrets — real API keys live in .env (never commit). .env.example is the tracked template.
.env
.env.*
!.env.example

# Cloned reference repos, scratch
oss/

# Personal scratch notes
NOTES.md
arch.md

# Local MCP server config (may contain machine-specific paths/secrets)
.mcp.json

.DS_Store
*.lock
