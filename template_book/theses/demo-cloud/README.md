# demo-cloud (MOCK)

MockCloud Inc (MCLD) — a fictional AI-infra compute provider. The bet: the market prices it on demand, but it is **supply-constrained**, so revenue misses are build-out delay, not weak demand. High conviction in this demo. Mock ticker `MCLD`, assumed annualized volatility σ = 0.45.

Not a real position — exists only to demonstrate the loop.

## Instructions for the next agent
Free-text the agent reads and honors before acting on this thesis.
- Multi-year hold: don't trim on a single weak quarter — only a load-bearing pillar actually breaking (score → 0) forces an exit.
- Express new size in MCLD common, not options.
